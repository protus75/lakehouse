# Base packages — shared by daemon and workspace images.
# Heavy dependencies that rarely change. Changing this file
# triggers a full rebuild of both downstream images.

# Data ingestion
dlt[duckdb,filesystem]==1.21.0

# Query engine
duckdb==1.4.4

# Table format
pyiceberg[duckdb,sql-postgres]==0.11.1

# Data processing
polars==1.30.0
pyarrow==23.0.1

# Database drivers
psycopg2-binary==2.9.10

# Utilities
requests==2.32.5
python-dotenv==1.2.1
pyyaml==6.0.2

# OCR validation
pyspellchecker==0.8.1
