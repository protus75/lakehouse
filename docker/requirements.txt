# Additional packages — lightweight, changes frequently.
# Base packages in requirements-base.txt (separate Docker layer).

# Utilities
requests==2.32.5
python-dotenv==1.2.1
pyyaml==6.0.2

# OCR validation
pyspellchecker==0.8.1
